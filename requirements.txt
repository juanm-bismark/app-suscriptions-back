# Framework core
fastapi>=0.130.0
uvicorn[standard]>=0.44.0

# Validaciones y settings
pydantic>=2.9.0
pydantic-settings>=2.14.0
python-dotenv>=1.2.0

# Cliente HTTP (adapters de proveedores)
httpx>=0.28.0

# Postgres support
SQLAlchemy[asyncio]>=2.0.0
asyncpg
alembic>=1.18.0

# Required by FastAPI para form/file parsing
python-multipart>=0.0.18

# Auth
PyJWT>=2.10.0
bcrypt>=4.0.0
cryptography>=43.0.0

# Logging estructurado
structlog>=24.0.0

# Resiliencia — retry con backoff exponencial (usado en adapters, Ola 1)
tenacity>=9.0.0

# API Features
fastapi-pagination>=0.15.0
